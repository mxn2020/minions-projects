[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-projects"
version = "0.2.0"
description = "Project definitions, variants, goals, status, and ownership"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["projects","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-projects"
Repository = "https://github.com/mxn2020/minions-projects"

[tool.hatch.build.targets.wheel]
packages = ["minions_projects"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
